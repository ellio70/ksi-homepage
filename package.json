{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "css:build": "tailwindcss -i ./src/styles/tailwind.css -o ./public/static/tailwind.css --minify",
    "css:watch": "tailwindcss -i ./src/styles/tailwind.css -o ./public/static/tailwind.css --watch",
    "build": "npm run css:build && vite build",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.12",
    "openai": "^6.39.0"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@tailwindcss/typography": "^0.5.19",
    "tailwindcss": "^3.4.19",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
